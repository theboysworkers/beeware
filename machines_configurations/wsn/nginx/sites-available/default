# This block defines a VirtualHost for the domain wsn.theboys.it (This domain is blocked)
server {
    listen 80; 
    listen [::]:80; 

    server_name wsn.theboys.it;

    root /var/www/html;
    index index.html;

    location / {
        deny all; 
    }
}

# This block defines a VirtualHost for the domain agency.theboys.it
server {
	listen 80;
	listen [::]:80;

	server_name agency.theboys.it;

	root /var/www/html/agency;
	index index.html;

	location / {
		try_files $uri $uri/ =404;
	}
}

# This block defines a VirtualHost for the domain ecopulse.theboys.it
server {
	listen 80;
	listen [::]:80;

	server_name ecopulse.theboys.it;

	root /var/www/html/ecopulse;
	index index.html;

	location / {
		try_files $uri $uri/ =404;
	}
}

# This block defines a VirtualHost for the domain myrecipe.theboys.it
server {
	listen 80;
	listen [::]:80;

	server_name myrecipe.theboys.it;

	root /var/www/html/myrecipe;
	index index.html;

	location / {
		try_files $uri $uri/ =404;
	}
}

# This block defines a VirtualHost for the domain puffcats.theboys.it
server {
	listen 80;
	listen [::]:80;

	server_name puffcats.theboys.it;

    root /var/www/html/puffcats;
    index index.php index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }
}
