# Load global options (defined in named.conf.options)
include "/etc/bind/named.conf.options";

# Root zone (".") — type "hint" means this file contains the list of root servers
zone "." {
    type hint;
    file "/etc/bind/zones/db.root";   # contains root DNS server addresses
};

# Authoritative zone for theboys.it
zone "theboys.it" {
    type master;                           # this server is the primary (master) for the zone
    file "/etc/bind/zones/db.it.theboys";  # zone data file with records for theboys.it
};

# Fake/blocked zone for facebook.com
# Social media
zone "youtube.com"    { type master; file "/etc/bind/zones/db.blocked"; };
zone "facebook.com"   { type master; file "/etc/bind/zones/db.blocked"; };
zone "tiktok.com"     { type master; file "/etc/bind/zones/db.blocked"; };
zone "instagram.com"  { type master; file "/etc/bind/zones/db.blocked"; };
zone "twitter.com"    { type master; file "/etc/bind/zones/db.blocked"; };
zone "reddit.com"     { type master; file "/etc/bind/zones/db.blocked"; };
# Adult / NSFW sites
zone "pornhub.com"    { type master; file "/etc/bind/zones/db.blocked"; };
zone "xvideos.com"    { type master; file "/etc/bind/zones/db.blocked"; };
zone "xnxx.com"       { type master; file "/etc/bind/zones/db.blocked"; };
zone "onlyfans.com"   { type master; file "/etc/bind/zones/db.blocked"; };
zone "redtube.com"    { type master; file "/etc/bind/zones/db.blocked"; };
zone "tube8.com"      { type master; file "/etc/bind/zones/db.blocked"; };

# Reverse DNS zone for subnet 2a04::/64 (IPv6 PTR records)
zone "4.0.a.2.ip6.arpa" {
    type master;
    file "/etc/bind/zones/db.2a04_rev";  # contains reverse DNS (PTR) mappings
};
